## Summary

Describe what this PR changes and why.

## Type of change

State whether this is a bug fix, a new feature, a breaking change, or a documentation-only update.

## How was this tested

Explain how you verified the change (unit tests, manual testing steps, screenshots, etc.).

## Checklist

`pnpm lint`, `pnpm -w typecheck`, and `pnpm test` all pass locally. Documentation (README, CONTRIBUTING, docs/) was updated if behavior or configuration changed. This PR does not include secrets, API keys, tokens, or other credentials. Any new environment variables are documented in `env.example`.

## Related issues

Link any related issue with "Closes #123" if applicable.
